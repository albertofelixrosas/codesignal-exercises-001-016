/*
Write a function that returns the sum of two numbers.

Example

For param1 = 1 and param2 = 2, the output should be solution(param1, param2) = 3.

Escribe una función que devuelva la suma de dos números.

Ejemplo

Para param1 = 1 y param2 = 2, la salida debe ser solution(param1, param2) = 3.
*/
const solution_001 = (param1: number, param2: number): number => {
    return param1 + param2;
};

console.log(5, 7);
